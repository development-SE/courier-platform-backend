spring.application.name=notification
# KAFKA_BOOTSTRAP_SERVERS  → your Kafka broker address
# MAIL_HOST / MAIL_USERNAME / MAIL_PASSWORD  → your SMTP credentials
# BASE_URL  → your auth-service URL (used in verify link)